//=======================================
// for control browser script cache
appConfig.scriptsCacheKey = 'v1';

//=======================================
// available languages
appConfig.languages = ['ru'/*,'en'*/];
// force language rather than auto
appConfig.setLanguage = 'ru',

//=======================================
// servers list
appConfig.servers = {
  backend: 'https://............./'
};